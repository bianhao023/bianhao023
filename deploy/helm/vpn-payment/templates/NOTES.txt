vpn-payment has been deployed.

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Image:     {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}

1. Check the rollout:
   kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "vpn-payment.fullname" . }}

2. Application secrets are NOT created by this chart.
   Ensure the Secret "{{ .Values.existingSecret }}" exists in namespace {{ .Release.Namespace }}
   with all required secret env vars (see deploy/k8s/secret.example.yaml).

{{- if .Values.ingress.enabled }}

3. Ingress is enabled. Once DNS points at your ingress controller, the app is at:
   https://{{ .Values.ingress.host }}{{ .Values.ingress.path }}
{{- else }}

3. Ingress is disabled. Reach the service in-cluster or via port-forward:
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "vpn-payment.fullname" . }} 8080:{{ .Values.service.port }}
   # then open http://127.0.0.1:8080/healthz
{{- end }}

{{- if .Values.autoscaling.enabled }}

4. Autoscaling is enabled ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} replicas, CPU target {{ .Values.autoscaling.targetCPUUtilizationPercentage }}%).
   Requires metrics-server.
{{- end }}

{{- if .Values.serviceMonitor.enabled }}

5. A ServiceMonitor was created. It requires the Prometheus Operator CRDs and a
   Prometheus whose serviceMonitorSelector matches these labels:
   {{- range $k, $v := .Values.serviceMonitor.labels }}
     {{ $k }}: {{ $v }}
   {{- end }}
{{- end }}
