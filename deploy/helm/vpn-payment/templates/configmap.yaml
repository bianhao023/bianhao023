apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vpn-payment.fullname" . }}-config
  labels:
    {{- include "vpn-payment.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
