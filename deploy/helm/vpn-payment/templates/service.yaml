apiVersion: v1
kind: Service
metadata:
  name: {{ include "vpn-payment.fullname" . }}
  labels:
    {{- include "vpn-payment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "vpn-payment.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
